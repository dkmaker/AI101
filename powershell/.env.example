# OpenRouter API Configuration
# Copy this file to .env and fill in your actual API key
# The .env file should be added to .gitignore to prevent committing sensitive data

# API Key for OpenRouter (required)
OPENROUTER_API_KEY=your_api_key_here

# Optional configuration overrides
# Uncomment and modify these lines to override settings in config.json
# OPENROUTER_MODEL=openai/chatgpt-4o-latest
# OPENROUTER_TEMPERATURE=0.7
# OPENROUTER_MAX_TOKENS=1000
